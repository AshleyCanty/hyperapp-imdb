import {h, app} from 'hyperapp'

export default function TopImg({state, actions}) {
     return (
          <section>
          <div class="container">
               <div class="wide">

               </div>

          </div>
    </section>
     )
}
